{"schemaVersion":2,"courseId":"var","module":{"id":"var-estimation-lag-selection","title":"2 · Estimation & lag selection","summary":"How a VAR is fit equation-by-equation with OLS, and how information criteria pick the lag length p.","stages":[{"type":"read","title":"Estimating a VAR is just OLS, k times","html":"<h2>Each equation is its own regression</h2><p>A VAR(p) regresses each variable on <code>p</code> lags of <em>all</em> variables. Because every equation has the <em>same</em> right-hand-side regressors (the stacked lags), running OLS separately on each equation is fully efficient — there is nothing to gain from a fancier joint estimator. So fitting a k-variable VAR(p) is literally <strong>k ordinary OLS regressions</strong>.</p><p>What ties the equations together is not the estimation but the <strong>residual covariance matrix</strong> <code>Σ</code>. The shocks <code>ε1ₜ</code> and <code>ε2ₜ</code> are usually correlated <em>within</em> a period (a surprise that hits output often hits inflation the same quarter). That contemporaneous correlation lives entirely in Σ and becomes crucial later for impulse responses.</p><h3>Choosing the lag length p</h3><p>How many lags? Too few and you miss real dynamics (biased, autocorrelated residuals); too many and you burn degrees of freedom estimating noise (a VAR(p) on k variables has <code>k·(k·p+1)</code> parameters — it grows fast). We trade fit against parsimony with <strong>information criteria</strong>:</p><ul><li><strong>AIC</strong> — light penalty, tends to pick more lags; good for forecasting.</li><li><strong>BIC</strong> (a.k.a. SC) — heavy penalty (∝ log n), picks fewer lags; consistent for the true order.</li><li><strong>HQIC</strong> — in between.</li><li><strong>FPE</strong> — final prediction error, close to AIC.</li></ul><div class=\"callout\"><b>Key idea.</b> Fit = k separate OLS regressions. Lag choice = minimize an information criterion that rewards fit and punishes the parameter count.</div>","id":"var-estimation-lag-selection-01","skillIds":["var.var-estimation"],"estimatedMinutes":6,"difficulty":"advanced","prerequisiteStageIds":["var-from-ar1-to-var-07"]},{"type":"code","title":"select_order: read the criteria table","note":"We simulate a true VAR(2) and print the IC table. Each criterion reports the lag it would choose. Watch BIC pick fewer lags than AIC.","code":"import numpy as np, pandas as pd\nfrom statsmodels.tsa.api import VAR\nrng = np.random.default_rng(0)\n\n# True process is VAR(2): A1 then A2 act on successive lags\nA1 = np.array([[0.5, 0.1],[0.0, 0.5]])\nA2 = np.array([[0.0, 0.2],[0.2, 0.0]])\nn = 1000\nY = np.zeros((n, 2))\nfor t in range(2, n):\n    Y[t] = A1 @ Y[t-1] + A2 @ Y[t-2] + rng.normal(0, 1, 2)\ndf = pd.DataFrame(Y, columns=['y1','y2'])\n\nsel = VAR(df).select_order(maxlags=8)\nprint(sel.summary())\nprint('\\nLag chosen by each criterion:')\nprint(sel.selected_orders)","id":"var-estimation-lag-selection-02","skillIds":["var.var-stability"],"estimatedMinutes":10,"difficulty":"advanced","prerequisiteStageIds":["var-estimation-lag-selection-01"]},{"type":"code","title":"Fit by an information criterion automatically","note":"Pass ic='aic' and statsmodels searches lags up to maxlags and keeps the best. Then we sanity-check the residuals are close to white noise.","code":"import numpy as np, pandas as pd\nfrom statsmodels.tsa.api import VAR\nfrom statsmodels.stats.stattools import durbin_watson\nrng = np.random.default_rng(0)\nA1 = np.array([[0.5, 0.1],[0.0, 0.5]])\nA2 = np.array([[0.0, 0.2],[0.2, 0.0]])\nn = 1000\nY = np.zeros((n, 2))\nfor t in range(2, n):\n    Y[t] = A1 @ Y[t-1] + A2 @ Y[t-2] + rng.normal(0, 1, 2)\ndf = pd.DataFrame(Y, columns=['y1','y2'])\n\nres = VAR(df).fit(maxlags=8, ic='aic')\nprint('Lag order chosen by AIC:', res.k_ar)\nprint('Shape of coefficient array res.coefs (p, k, k):', res.coefs.shape)\nprint('\\nResidual covariance Sigma:\\n', np.round(res.sigma_u, 3))\nprint('\\nDurbin-Watson per equation (near 2 = no leftover autocorrelation):')\nprint(np.round(durbin_watson(res.resid.values), 2))","id":"var-estimation-lag-selection-03","skillIds":["var.var-estimation"],"estimatedMinutes":10,"difficulty":"advanced","prerequisiteStageIds":["var-estimation-lag-selection-02"]},{"type":"interactive","title":"Sample size vs the lag BIC picks","note":"The data are truly VAR(2). With few observations, criteria get noisy; as n grows, BIC should home in on lag 2. Move the slider and read the chosen lags in the title.","params":[{"name":"n","label":"sample size n","min":80,"max":1000,"step":20,"value":200}],"template":"import numpy as np, pandas as pd, matplotlib.pyplot as plt\nfrom statsmodels.tsa.api import VAR\nrng = np.random.default_rng(0)\nA1 = np.array([[0.5, 0.1],[0.0, 0.5]])\nA2 = np.array([[0.0, 0.2],[0.2, 0.0]])\nn = int({{n}})\nY = np.zeros((n, 2))\nfor t in range(2, n):\n    Y[t] = A1 @ Y[t-1] + A2 @ Y[t-2] + rng.normal(0, 1, 2)\ndf = pd.DataFrame(Y, columns=['y1','y2'])\nmaxlags = 8\nsel = VAR(df).select_order(maxlags=maxlags)\naics, bics = [], []\nbase = VAR(df)\nfor p in range(1, maxlags+1):\n    r = base.fit(p)\n    aics.append(r.aic); bics.append(r.bic)\nlags = np.arange(1, maxlags+1)\nplt.figure(figsize=(6,3.7))\nplt.plot(lags, aics, 'o-', color='#da9100', label='AIC')\nplt.plot(lags, bics, 's-', color='#6f6b57', label='BIC')\nplt.axvline(2, color='#c9c6ac', ls='--', lw=1)\nplt.xlabel('lag p'); plt.legend(fontsize=8)\nplt.title(f\"n={n}: AIC picks {sel.selected_orders['aic']}, BIC picks {sel.selected_orders['bic']} (truth=2)\")","id":"var-estimation-lag-selection-04","skillIds":["var.var-stability"],"estimatedMinutes":8,"difficulty":"advanced","prerequisiteStageIds":["var-estimation-lag-selection-03"]},{"type":"quiz","title":"Check: which criterion?","prompt":"You care most about not overfitting and want the most parsimonious lag order that is consistent for the true p. Which criterion do you lean on?","choices":["AIC","BIC (SC)","FPE","always use the maximum lag"],"answer":1,"hint":"One of these has a penalty that grows with log(n) and is the strictest.","explain":"BIC's penalty (∝ log n · #params) is the harshest, so it selects fewer lags and is consistent for the true order; AIC/FPE favor richer models better suited to pure forecasting.","id":"var-estimation-lag-selection-05","skillIds":["var.var-estimation"],"estimatedMinutes":5,"difficulty":"advanced","prerequisiteStageIds":["var-estimation-lag-selection-04"],"variantId":"var-estimation-lag-selection-05-core"},{"type":"quiz","title":"Check: estimation method","prompt":"Why can a VAR be estimated by running OLS separately on each equation without losing efficiency?","choices":["Because the errors are independent across equations","Because every equation shares exactly the same set of regressors (the stacked lags)","Because OLS is always efficient","Because the residual covariance Σ is diagonal"],"answer":1,"hint":"Think about what SUR/GLS would buy you when all equations have identical regressors.","explain":"When every equation has the same regressors, the GLS (SUR) estimator collapses to equation-by-equation OLS, so separate OLS is already fully efficient even though the errors are correlated across equations.","id":"var-estimation-lag-selection-06","skillIds":["var.var-stability"],"estimatedMinutes":5,"difficulty":"advanced","prerequisiteStageIds":["var-estimation-lag-selection-05"],"variantId":"var-estimation-lag-selection-06-core"},{"type":"fillblank","title":"Which criterion picks the fewest lags?","prompt":"Among AIC, BIC, and HQIC, the criterion with the harshest penalty (∝ log n · #params) — so it selects the fewest lags and is consistent for the true order — is ___.","lead":"select_order printed AIC, BIC, and HQIC side by side; you must name the one that penalizes extra parameters hardest.","accept":["bic","sc","schwarz","schwarz criterion","schwarz information criterion","bayesian information criterion"],"hint":"Its penalty grows with log n, so it punishes extra parameters harder than AIC as the sample grows.","explain":"<b>The all-criteria-agree assumption.</b> BIC (a.k.a. SC / Schwarz) carries the heaviest penalty (∝ log n per parameter), so it selects fewer lags and is consistent for the true lag order. AIC and FPE use a lighter, fixed penalty and tend to pick more lags — better for pure forecasting, not for recovering the true order. <em>Boundary:</em> when the goal is forecasting rather than identifying the true p, the richer AIC model can legitimately win, so the criteria need not, and often do not, agree.","points":15,"id":"var-estimation-lag-selection-07","skillIds":["var.var-estimation"],"estimatedMinutes":4,"difficulty":"advanced","prerequisiteStageIds":["var-estimation-lag-selection-06"],"variantId":"var-estimation-lag-selection-07-core"}]}}
