{"schemaVersion":2,"courseId":"did","module":{"id":"did-2x2","title":"2 · The 2×2 estimator","summary":"The DiD estimate is exactly the interaction coefficient in a regression of y on treated, post, and their product.","stages":[{"type":"read","title":"DiD is one regression coefficient","html":"<h2>From four means to a regression</h2><p>Computing four cell means and differencing is intuitive, but a single regression gives the same number <em>plus</em> a standard error, a t-statistic, and the ability to add controls. We regress the outcome on two dummies and their interaction:</p><p class=\"katexish\">y = β₀ + β₁·treated + β₂·post + β₃·(treated·post) + ε</p><p>Read each coefficient off the 2×2 grid:</p><ul><li><strong>β₀</strong> = control group, before (the baseline cell).</li><li><strong>β₁</strong> = treated-vs-control gap <em>before</em> treatment (a level difference).</li><li><strong>β₂</strong> = before-vs-after change in the <em>control</em> group (the common trend).</li><li><strong>β₃</strong> = the <em>extra</em> change in the treated group on top of the trend — this is the <strong>DiD / ATT</strong>.</li></ul><div class=\"callout\"><b>Key idea.</b> The interaction term <code>treated:post</code> is the difference-in-differences. Its coefficient β₃ is the ATT, and its standard error gives you inference for free.</div>","id":"did-2x2-01","skillIds":["did.did-regression"],"estimatedMinutes":6,"difficulty":"applied","prerequisiteStageIds":["did-idea-06"]},{"type":"read","title":"Why the interaction equals the double difference","html":"<h3>Plug in the four cells</h3><p>Evaluate the regression equation at each cell (ignoring ε, which averages to zero):</p><ul><li>control, before: ŷ = β₀</li><li>control, after: ŷ = β₀ + β₂</li><li>treated, before: ŷ = β₀ + β₁</li><li>treated, after: ŷ = β₀ + β₁ + β₂ + β₃</li></ul><p>Now form the double difference:</p><p class=\"katexish\">(treated,after − treated,before) − (control,after − control,before)</p><p class=\"katexish\">= (β₂ + β₃) − (β₂) = β₃</p><p>Everything except β₃ cancels. The regression is algebraically identical to the hand calculation — but it scales to many covariates and clustered standard errors.</p><div class=\"callout\"><b>Key idea.</b> <code>smf.ols(\"y ~ treated*post\", df)</code> in statsmodels automatically adds <code>treated</code>, <code>post</code>, and <code>treated:post</code>. Just read off <code>params['treated:post']</code>.</div>","id":"did-2x2-02","skillIds":["did.parallel-trends"],"estimatedMinutes":6,"difficulty":"applied","prerequisiteStageIds":["did-2x2-01"]},{"type":"code","title":"Estimate the ATT with statsmodels","note":"Same simulated data as Module 1. The interaction coefficient matches the hand-computed DiD exactly, and now we get a standard error and p-value.","code":"import numpy as np, pandas as pd\nimport statsmodels.formula.api as smf\nrng = np.random.default_rng(0)\nn = 500\ntreated = rng.integers(0,2,n); post = rng.integers(0,2,n)\nATT = 4.0\ny = 10 + 3*treated + 2*post + ATT*treated*post + rng.normal(0,2,n)\ndf = pd.DataFrame(dict(y=y, treated=treated, post=post))\n\nres = smf.ols('y ~ treated*post', df).fit()\nprint(res.summary().tables[1])\nprint()\nprint(f\"ATT (treated:post) = {res.params['treated:post']:.3f}\")\nprint(f\"std error          = {res.bse['treated:post']:.3f}\")\nprint(f\"true ATT           = {ATT}\")","id":"did-2x2-03","skillIds":["did.did-regression"],"estimatedMinutes":10,"difficulty":"applied","prerequisiteStageIds":["did-2x2-02"]},{"type":"interactive","title":"Noise vs. precision of the ATT","note":"Crank up the idiosyncratic noise σ and watch the point estimate stay roughly right while its standard error balloons.","params":[{"name":"att","label":"True ATT","min":-5,"max":8,"step":0.5,"value":4},{"name":"noise","label":"Noise σ","min":0.5,"max":8,"step":0.5,"value":2}],"template":"import numpy as np, pandas as pd, matplotlib.pyplot as plt\nimport statsmodels.formula.api as smf\nrng = np.random.default_rng(0)\nn = 600\ntreated = rng.integers(0,2,n); post = rng.integers(0,2,n)\ny = 10 + 3*treated + 2*post + {{att}}*treated*post + rng.normal(0,{{noise}},n)\ndf = pd.DataFrame(dict(y=y, treated=treated, post=post))\nres = smf.ols('y ~ treated*post', df).fit()\nb = res.params['treated:post']; se = res.bse['treated:post']\nplt.figure(figsize=(6,3.7))\nplt.axhline({{att}}, color='#6f6b57', ls='--', lw=1.5, label='true ATT')\nplt.errorbar([0], [b], yerr=[1.96*se], fmt='o', color='#da9100', ecolor='#c9c6ac', capsize=6, ms=9, label='estimate +/- 95% CI')\nplt.xlim(-1,1); plt.xticks([]); plt.legend(fontsize=8)\nplt.title(f'ATT_hat = {b:.2f}   SE = {se:.3f}')","id":"did-2x2-04","skillIds":["did.parallel-trends"],"estimatedMinutes":8,"difficulty":"applied","prerequisiteStageIds":["did-2x2-03"]},{"type":"quiz","title":"Check","prompt":"In the regression y ~ treated*post, which coefficient is the causal DiD estimate (the ATT)?","choices":["The coefficient on 'treated'","The coefficient on 'post'","The coefficient on 'treated:post'","The intercept"],"answer":2,"hint":"Which coefficient survives the double difference after the others cancel?","explain":"Only the interaction 'treated:post' captures the extra change in the treated group beyond the common trend; the other terms cancel in the difference-of-differences.","id":"did-2x2-05","skillIds":["did.did-regression"],"estimatedMinutes":5,"difficulty":"applied","prerequisiteStageIds":["did-2x2-04"],"variantId":"did-2x2-05-core"},{"type":"quiz","title":"Check","prompt":"What does the coefficient on 'treated' (β₁) by itself represent?","choices":["The treatment effect","The pre-treatment level gap between treated and control groups","The common time trend","The post-period outcome of the control group"],"answer":1,"hint":"Set post = 0 and compare treated to control.","explain":"β₁ is the baseline (before-period) difference in levels between the groups. It is a fixed gap, not an effect — DiD explicitly allows the groups to differ in levels.","id":"did-2x2-06","skillIds":["did.parallel-trends"],"estimatedMinutes":5,"difficulty":"applied","prerequisiteStageIds":["did-2x2-05"],"variantId":"did-2x2-06-core"},{"type":"truefalse","title":"Equal pre-treatment levels?","prompt":"DiD requires the treated and control groups to have <b>equal outcome levels</b> before treatment.","answer":false,"hint":"Which coefficient in y ~ treated*post absorbs a fixed before-period gap between the groups?","explain":"The levels-vs-trends confusion. DiD <em>allows</em> any baseline level gap — that gap is exactly what β₁ (the 'treated' coefficient) absorbs, which is why the module called it a fixed level difference, not an effect. The tempting belief — that the groups must start equal — confuses a constant level gap (harmless, differenced away) with a difference in <b>trends</b> (fatal). Boundary: different starting <em>levels</em> are fine; differently-sloped <em>trends</em> break identification. Pivot word: levels (ok) vs trends (not ok).","points":10,"id":"did-2x2-07","skillIds":["did.did-regression"],"estimatedMinutes":3,"difficulty":"applied","prerequisiteStageIds":["did-2x2-06"],"variantId":"did-2x2-07-core"}]}}
